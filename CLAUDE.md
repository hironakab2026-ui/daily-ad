# CLAUDE.md — 日常生活管理システム

Claude Code へのプロジェクト引き渡し文書。作業開始前に必ず読むこと。

---

## 1. このプロジェクトは何か

日々の記録と月次の振り返りを通じて QOL の向上を目指す、個人向けの生活管理システム。

**ハブ＆アドオン構成**をとる。

| # | アプリ | 責務 | 状態 |
| --- | --- | --- | --- |
| APP-1 | 生活管理アプリ（ハブ） | **収支管理・タスク管理**＋振り返りシート | 実装済み（9画面・マルチユーザー対応・デザイン刷新済み） |
| APP-2 | 食事管理アプリ | 食事・体重の記録、栄養評価 | 実装・接続済み（外部接続型、別リポジトリ） |
| APP-3 | 運動管理アプリ | 筋トレの記録、評価 | 実装・接続済み（外部接続型、別リポジトリ） |
| APP-4 | スケジュール管理アプリ | 予定・カレンダー（週/日/月表示） | 実装済み（内蔵型、本リポジトリに同梱） |

2026-09-18時点でこの4本が稼働中。**本リポジトリ（daily-ad）の実装対象は APP-1 と、内蔵型で組み込まれた APP-4。** APP-2 / APP-3 のコードは別リポジトリのため本リポジトリでは書かない。今後 APP-5 以降を追加する場合は、まず `00_システム全体構成・連携仕様.md` 7章の手順に従うこと（外部接続型か内蔵型かをそこで判断する）。

---

## 2. 必読ドキュメント

| ファイル | 内容 |
| --- | --- |
| `00_システム全体構成・連携仕様.md` | アーキテクチャ、アドオン連携仕様（2026-09-18更新・APP-2〜4接続済み、APP-5以降の追加手順あり） |
| `01_要件定義書_生活管理アプリ.md` | **APP-1 の要件（最重要）。実装完了後にas-builtとして書き起こし済み** |
| `02_要件定義書_食事管理アプリ.md` | APP-2 の要件。接続・実装済み |
| `03_要件定義書_運動管理アプリ.md` | APP-3 の要件。接続・実装済み |
| `04_要件定義書_スケジュール管理アプリ.md` | APP-4 の要件。実装済み（本リポジトリに同梱） |
| `05_データベース選定メモ.md` | DB の選定材料（決定：MariaDB、`05_service_connections.sql`まで運用中） |
| `db/01_schema.sql`〜`db/06_schedule.sql` | MariaDB 版スキーマ一式（番号順に適用する。詳細は `README.md`） |

要件 ID（`F-01-01` など）は要件定義書と対応している。実装時はコミットメッセージや PR に ID を書くこと。

---

## 3. 技術スタック

**この章は初期検討時点の記録として残す。実際にはB案（サーバーDBのみ・MariaDB）で決定・実装済みで、以下は当時の判断材料。現状の構成は `00_システム全体構成・連携仕様.md` 3章を参照。**

判断材料は `05_データベース選定メモ.md` に整理済み。要点のみ再掲する。

### 先に決めるべきは「オフライン対応の要否」
| 案 | 構成 | オフライン | 難易度 |
| --- | --- | --- | --- |
| A | SQLite のみ | ○ | 低 |
| B | サーバーDB のみ（MariaDB 等） | × | 低〜中 |
| C | SQLite ＋ サーバーDB ＋ 同期 | ○ | 高 |

要件定義書 C-05〜C-06 は案 C を前提に書かれているが、**学習段階では A または B から始めてよい**とユーザーと合意済み。

### スキーマは2種類用意済み
| ディレクトリ | 対象 | 状態 |
| --- | --- | --- |
| `db/` | SQLite | **検証済み**（予定生成・集計・制約チェックまで動作確認） |
| `db_mariadb/` | MariaDB 10.6+ | **実行未検証**。最初に流したときエラーが出たら直すこと |

両者はテーブル構造・制約・ビューの設計が同一で、方言の差のみを吸収してある。PostgreSQL へ移す場合の変換点は `05_データベース選定メモ.md` 第6章にある。

---

## 4. 絶対に守る設計上の制約

ユーザーが明示的に決定した事項。**勝手に変えないこと。**

### 4.1 APP-1 の責務は収支管理とタスク管理に限定する
- 食事・体重・運動の記録機能を APP-1 に**実装しない**
- 他コンテンツの**詳細を描画しない**（栄養グラフ、時系列リスト等）
- 他コンテンツのデータを**編集しない**
- 受け取るのは要約（grade ＋ metrics 最大6件）のみ

### 4.2 画面数は9。増やさない
`01_要件定義書_生活管理アプリ.md` 第5章の一覧がすべて。機能追加の要望が出たら、まずコンテンツアプリ側で実現できないか検討する。

### 4.3 タスクは削除せず、実行期間で制御する
- `end_date` を設定することで「やめる」を表現する
- 物理削除は設定画面の奥にのみ置き、過去記録も消える旨の確認を挟む
- **過去の `task_logs` は定義を変更しても一切変わらないこと**

### 4.4 タスクの「予定」は行として先に作る
`task_logs` に `is_done = 0` の行を生成する方式。繰り返し条件からその都度計算する方式にしない。完遂率の分母を SQL で出せなくなるため。

### 4.5 削除は論理削除
`deleted_at` に時刻を入れる。物理削除しない（同期で削除を伝播させるため）。

### 4.6 予算機能は作らない
ユーザーが明示的に対象外と決定済み。振り返りが基軸であり、事前の予算統制は有効性が低いと判断された。

---

## 5. データベース

SQLite 版（`db/01_schema.sql`）は**検証済み**。7日分の予定生成、集計、制約チェックまで動作確認している。MariaDB 版（`db_mariadb/`）は設計は同一だが**実行未検証**。

どちらも勝手に作り替えないこと。変更が必要な場合はユーザーに確認する。

### テーブル
| テーブル | 用途 |
| --- | --- |
| `categories` | 収支カテゴリ |
| `transactions` | 収支記録 |
| `task_definitions` | タスク定義（繰り返し条件・実行期間） |
| `task_logs` | タスクの予定と実行記録 |
| `content_summary_cache` | コンテンツ要約のキャッシュ（Phase 3 以降） |

### ビュー
| ビュー | 用途 |
| --- | --- |
| `v_daily_balance` | 日ごとの収入・支出・収支差 |
| `v_daily_task` | 日ごとの完遂率 |
| `v_calendar_cell` | カレンダーのセル用（上記2つを結合） |

### 実装時の注意
- 金額は円単位の整数。浮動小数点を使わない
- 真偽値は 0 / 1
- **SQLite を使う場合**：接続のたびに `PRAGMA foreign_keys = ON;` を実行する（既定は無効）。日付は `'YYYY-MM-DD'` の文字列で持つ
- **MariaDB を使う場合**：`DAYOFWEEK()` は 1=日曜 … 7=土曜 を返す（SQLite の `strftime('%w')` は 0=日曜。ずれるので注意）。部分インデックスは使えないため、論理削除の絞り込みは複合インデックスで代替している
- MariaDB の `UPDATE` は左から順に評価される。実行ボタンのトグル（Q3）では `completed_at` を `is_done` より先に書くこと

---

## 6. 実装順序

`01_要件定義書_生活管理アプリ.md` の要件 ID に対応。

### Phase 1-a：収支管理
0. **DB とフレームワークの決定**（ユーザーに確認）
1. DB 初期化（スキーマ適用・初期カテゴリ投入）
2. 収支入力画面（A1-02） — F-01-01〜07
3. 当日サマリー（A1-01）の収支部分
4. カテゴリ管理（A1-09 の一部）

### Phase 1-b：タスク管理
5. タスク定義画面（A1-04） — F-02-01〜12
6. 予定生成ロジック（`db/03_queries.sql` の Q1）
7. 当日タスク一覧（A1-03）
8. リマインド — F-02-13〜18

### Phase 1-c：振り返り
9. カレンダー（A1-05） — F-03-01〜07、自アプリ分のみ
10. 日別詳細（A1-06） — 収支・タスクのみ
11. 月次サマリー（A1-07）

### Phase 2 以降
12. 記録一覧・一括編集（A1-08）— Web 優先
13. サーバー同期（PostgreSQL）
14. アドオン連携（APP-2 の接続）

**Phase 1 完了の定義：コンテンツアプリが1つも接続されていない状態で、収支管理とタスク管理のアプリとして日常的に使えること。**

---

## 7. 性能上の要件

- カレンダー表示は、**ローカル問い合わせ＋コンテンツ要約の月次取得1回**で完結すること
- 日別画面を開くたびの通信を行わない
- 収支入力は起動から保存完了まで3タップ・15秒以内
- 起動直後にテンキーを表示する

---

## 8. 着手前に確認すべきこと

**アプリのフレームワークが未確定。** ユーザーに確認してから着手すること。

| 候補 | SQLite ライブラリ | 備考 |
| --- | --- | --- |
| Flutter | `drift` または `sqflite` | iOS / Android / Web を1コードベースで。型安全な SQL が書ける |
| React Native (Expo) | `expo-sqlite` | TypeScript 経験があるなら |
| Web のみ先行 | `better-sqlite3`（Node） | 学習目的ならこれが最速。あとからモバイル化 |

学習目的が主なら、**Web 版を先に作る**選択肢も有力。ブラウザで結果が見えるため試行錯誤が速い。

---

## 9. 作業の進め方

- 要件 ID を参照しながら実装する
- 要件にないものを勝手に足さない。必要だと思ったらユーザーに提案する
- スキーマ変更が必要になったら、実装を進める前にユーザーに確認する
- ユーザーは**学習目的**を明言している。何をしているか説明しながら進め、生成したコードの意図を伝えること
