# システム全体構成・連携仕様

| 項目 | 内容 |
| --- | --- |
| 作成日 | 2026-09-17 |
| 更新日 | 2026-09-18（APP-4 スケジュール管理アプリの追加、今後のアプリ追加手順を反映） |
| 状態 | **確定。** APP-2〜APP-4 は本ドキュメントの方針で実装・接続済み |
| 対象読者 | APP-1（本リポジトリ）およびアドオンアプリを実装する開発者（Claude Code 含む）。**今後新しいアプリ（APP-5以降）を追加する際も、まずこのドキュメント、特に7章を読むこと** |

---

## 1. 全体像

日常生活管理システムは、**ハブ＆アドオン構成**を取る。2026-09-18時点でアドオンは3本（食事・運動・スケジュール）が稼働しており、**今後も同じ型でアプリを追加していく**前提の構成である。

```
                     ┌─────────────────────────┐
                     │   APP-1 生活管理アプリ    │  ← ハブ（本リポジトリ daily-ad）
                     │   収支管理・タスク管理    │
                     │   ＋ 他アプリの要約表示   │
                     └────────────┬────────────┘
                                  │ 読むだけ（grade + metrics 最大6件）
                                  │ content_summary_cache 経由
                  ┌───────────────┼───────────────┬───────────────┐
                  │                               │               │
      ┌───────────▼───────────┐       ┌───────────▼───────────┐   │
      │  APP-2 食事管理アプリ  │       │  APP-3 運動管理アプリ  │   │  ← 外部接続型
      │  食事・体重・栄養評価  │       │  筋トレの記録・評価    │   │    （別リポジトリ、
      │  （別リポジトリ）      │       │  （別リポジトリ）      │   │     トークン認証）
      └────────────────────────┘       └────────────────────────┘   │
                                                                      │
                                                   ┌──────────────────▼──────────────────┐
                                                   │  APP-4 スケジュール管理アプリ         │  ← 内蔵型
                                                   │  予定・カレンダー（週/日/月表示）     │    （本リポジトリに
                                                   │  （本リポジトリ daily-ad に同梱）     │     同梱、直接書き込み）
                                                   └────────────────────────────────────────┘

                                                   APP-5以降 …（7章の手順で同じ型に追加していく）
```

- **ハブ（APP-1）**は収支とタスクの「一次データ」を持ち、それ以外のコンテンツ（食事・運動・スケジュールなど）については**要約だけ**を表示する。
- **アドオン**は自分の記録データを自分だけで持ち、ハブに対しては要約（`content_summary_cache` の1行）を書き込む。接続方式は2パターンある（詳細は7章）。
  - **外部接続型**（APP-2 食事、APP-3 運動）：別リポジトリ・別コードベースとして実装し、ハブが発行するトークンで `/api/integrations/summary` に要約を push する
  - **内蔵型**（APP-4 スケジュール）：本リポジトリ（daily-ad）にルート・画面ごと同梱し、`content_summary_cache` に直接書き込む
- 依存の向きは常に「アドオン → ハブ」の一方向。**ハブがアドオンの一次データを直接読み書きすることはない**（`content_summary_cache` を除く）。これは接続方式によらず共通のルール。

---

## 2. 各アプリの責務分担

| # | アプリ | 接続方式 | 持つデータ | ハブに渡すもの | 状態 |
| --- | --- | --- | --- | --- | --- |
| APP-1 | 生活管理アプリ（ハブ） | — | 収支（`transactions`）、タスク（`task_definitions` / `task_logs`）、カテゴリ | — | 稼働中 |
| APP-2 | 食事管理アプリ | 外部接続型（トークン） | 食事記録、体重記録、栄養評価ロジック（別リポジトリ） | `content_summary_cache`（`content_key='meal'`） | **接続済み・稼働中**（実データ流通確認済み） |
| APP-3 | 運動管理アプリ | 外部接続型（トークン） | 筋トレ記録、種目マスタ、評価ロジック（別リポジトリ） | `content_summary_cache`（`content_key='workout'`） | **接続済み・稼働中**（実データ流通確認済み） |
| APP-4 | スケジュール管理アプリ | 内蔵型（同一リポジトリ） | 予定（`schedule_calendars` / `schedule_definitions` / `schedule_occurrences`、本リポジトリ内） | `content_summary_cache`（`content_key='schedule'`） | **稼働中**（本リポジトリに同梱） |
| APP-5〜 | （今後追加）| 実装時に選択 | — | `content_summary_cache`（新しい `content_key`） | 未定。7章の手順で追加する |

ハブから見えるのは、接続方式によらずどのアプリも **grade・badge・metrics最大6件・deep_link のみ**。`CLAUDE.md` 4.1 の制約（詳細を描画しない・編集しない）は本仕様が守るべき最上位のルールであり、アドオン側の実装もこれを前提にする。

---

## 3. 現在の技術構成（実装済み・2026-09-17時点）

`05_データベース選定メモ.md` では案A（SQLiteのみ）〜案C（同期あり）を比較していたが、学習目的と「まず動くデモが欲しい」という要望から、**案B（サーバーDBのみ・オフライン非対応）**でAPP-1を実装済み。

| 項目 | 現状 |
| --- | --- |
| バックエンド | Node.js + Express |
| データベース | MariaDB 11.8（ローカル1台。ホスト名 `localhost`、DB名 `life_manager`） |
| フロントエンド | Vanilla JS（ビルドステップなし） |
| 認証 | **ログイン画面なし。** 初回アクセス時に `users` 行を自動作成し、署名付きCookie（`device_id`）で端末を識別する（`server/middleware/auth.js`）。メール・パスワードは持たない |
| マルチユーザー対応 | 済み。`categories` / `transactions` / `task_definitions` / `task_logs` / `content_summary_cache` すべてに `user_id` を付与済み（`db/04_multiuser.sql`）。「ユーザー」の実体は「端末（ブラウザ）」であり、人間が明示的にログインする概念はない |

**アドオンのDB方針（確定・実績あり）**：学習目的かつ個人利用規模のため、アドオンは**同じMariaDBインスタンス・同じ`life_manager`データベース・同じ`users`テーブル**を共有する。APP-2（食事）・APP-3（運動）・APP-4（スケジュール）の3本ともこの方針で実装され、問題なく稼働している。理由：
- 認証を1つに保てる（ユーザーIDの突き合わせが不要。ハブの `users.id` をそのまま使う）
- 個人利用規模ではDBを分ける運用コストに見合わない
- 将来分離したくなった場合も、`content_summary_cache` 経由の連携という境界は変わらないため移行しやすい

外部接続型（APP-2/APP-3）は別リポジトリでありながら同じDBに直接接続してテーブルを持ち、`user_id` はハブの `users.id` をそのまま使っている。内蔵型（APP-4）は当然ながら同じDBを使う。**今後追加するアプリも、特別な理由がない限りこの方針を踏襲する**（別DB・別サーバーに分ける場合は `content_summary_cache.user_id` の突き合わせ方法を別途決める必要があり、複雑になるため非推奨）。

---

## 4. 連携の仕組み：`content_summary_cache`

既存スキーマ（`db/01_schema.sql` → `db/04_multiuser.sql` で `user_id` 追加済み）をそのまま連携インターフェースとして使う。

```sql
content_summary_cache (
    user_id     INT          -- ハブの users.id と一致させる
    content_key VARCHAR(30)  -- 'meal' | 'workout' など、アドオンごとに固定の文字列
    target_date DATE         -- 'YYYY-MM-DD'
    status      VARCHAR(10)  -- 'none' | 'partial' | 'recorded'
    grade       VARCHAR(2)   -- 例: 'S','A','B','C'（アドオン側で採点基準を定義）
    badge       VARCHAR(20)  -- 例: '3日連続達成' など短いラベル（任意）
    metrics     JSON         -- 最大6件のキー・バリュー（例: {"kcal":1850,"protein_g":72}）
    deep_link   VARCHAR(255) -- アドオン側の該当日画面へのURL（ハブはこれを開くだけ）
    fetched_at  DATETIME
    PRIMARY KEY (user_id, content_key, target_date)
)
```

### 4.1 書き込み側（アドオン）のルール
- 記録が作成・更新されたタイミングで、**その日の行を1回 UPSERT する**（`INSERT ... ON DUPLICATE KEY UPDATE`）。
- `metrics` は**6キーまで**。7つ目以降は捨てるか、アドオン側でまとめる（例：個別の食事3件ではなく「合計カロリー」に集約）。
- `grade` の採点基準はアドオン側の要件定義書（`02_要件定義書_食事管理アプリ.md` など）に従う。ハブ側では採点ロジックを一切持たない。
- `deep_link` はハブのドメインに依存しない絶対URL、またはアプリ間遷移の仕組みが決まるまでは `null` でよい。

### 4.2 読み込み側（ハブ）のルール
- カレンダー画面（A1-05）は**月初に1回**、その月の `content_summary_cache` をまとめて取得する（`00` 制約と同じく、日ごとの通信をしない。`01_要件定義書_生活管理アプリ.md` 第7章の性能要件を参照）。
- 日別詳細（A1-06）・当日サマリー（A1-01）では、grade・badge・metrics（最大6件）をカード表示するのみ。**表・グラフ化や時系列描画はしない**（4.1 の制約）。
- metrics のキー名はアドオンごとに固定の辞書を要件定義書側で定義し、ハブは「キー名→表示ラベル」の対応表を持つだけにする（例: `kcal` → 「摂取カロリー」）。この対応表もハブ側のコードで管理する（DBに持たせない）。

### 4.3 決定・実績（2026-09-18更新）
- 認証は共有DBの `users` テーブルに一本化**済み**（3章）。APP-2/APP-3/APP-4とも同じ `users.id` を使っている
- 同期のタイミングは「アドオンの記録保存の都度、同期的に該当日を再計算してUPSERT」で**実装・運用中**（F-07-02 等に対応）
- grade は必須ではないと判明：APP-4（スケジュール）は grade を使わず `status` と `metrics` のみで運用している。**grade は「採点になじむアプリだけが使う任意項目」**という扱いに整理する

### 4.4 引き続き未決定の事項
- metrics のキー命名規則（アプリごとに接頭辞をつけるか等）。現状は各アプリの要件定義書内で個別に定義しているのみ
- grade を使う場合の値域を全アプリ共通にするか、アプリごとに自由にするか

新しいアプリを追加する際にこれらで迷った場合は、実装前にユーザーと合意の上で確定させること（`CLAUDE.md` 9章の方針に従う）。

---

## 5. フェーズ計画（`CLAUDE.md` 6章の再掲＋現状）

| フェーズ | 内容 | 状態 |
| --- | --- | --- |
| Phase 1 | APP-1単体（収支・タスク・振り返り）が、コンテンツアプリなしで日常的に使えること | 完了（9画面実装・マルチユーザー対応・デザイン刷新まで実施） |
| Phase 2 | 記録一覧・一括編集の強化、サーバー同期の高度化 | 一部着手（記録一覧の基本機能のみ） |
| Phase 3 | APP-2（食事管理）の接続 | **完了。** 接続・実データ連携を確認済み |
| Phase 4 | APP-3（運動管理）の接続 | **完了。** 接続・実データ連携を確認済み |
| Phase 5 | APP-4（スケジュール管理）の組み込み | **完了。** 本リポジトリに同梱、週/日/月表示まで実装済み |
| Phase 6以降 | 新しいアプリ（APP-5〜）の追加 | 未定。7章の手順で都度追加していく |

**Phase 1完了の定義（`CLAUDE.md`より）**：コンテンツアプリが1つも接続されていない状態で、収支管理とタスク管理のアプリとして日常的に使えること。この定義は達成済み。

---

## 6. 現在稼働しているアプリ（2026-09-18時点）

| アプリ | content_key | 接続方式 | 要件定義書 |
| --- | --- | --- | --- |
| 食事管理アプリ（APP-2） | `meal` | 外部接続型 | `02_要件定義書_食事管理アプリ.md` |
| 運動管理アプリ（APP-3） | `workout` | 外部接続型 | `03_要件定義書_運動管理アプリ.md` |
| スケジュール管理アプリ（APP-4） | `schedule` | 内蔵型 | `04_要件定義書_スケジュール管理アプリ.md` |

これが2026-09-18時点で「追加機能」として作成・接続済みのアプリの全量。新しいアプリを追加する場合は、下の7章の手順に従い、この表にも1行追加すること。

---

## 7. 新しいアプリを追加する手順（APP-5以降）

本システムは今後もアプリが増えていく前提で作られている。新しいアプリ（食事・運動・スケジュール以外の何か）を追加するときは、以下の手順で進める。**要件定義書ごとに個別の連携方式を発明しない**こと。

### 7.1 最初に決めること
1. **アプリ名と `content_key` を決める**（例：`reading`、`finance-goal` 等）。既存の `meal` / `workout` / `schedule` と衝突しない一意な英数字文字列にする
2. **接続方式を選ぶ**
   - **外部接続型**（推奨・デフォルト）：別リポジトリとして実装し、トークンでハブに接続する。APP-2/APP-3と同じ
   - **内蔵型**：本リポジトリ（daily-ad）にルート・画面ごと追加する。ハブの画面数・ナビが増える。APP-4と同じ。学習目的で「まずハブと一体で動かしたい」場合や、ごく小さい機能の場合に選ぶ
3. **要件定義書を作る**：`0N_要件定義書_◯◯アプリ.md`（Nは次の空き番号）として本リポジトリのルートに追加する。既存の `02` `03` `04` を雛形にする。最低限、以下を含めること
   - 目的・責務範囲（やること／やらないこと）
   - 画面一覧（画面数を明記し、確定後は増やさない）
   - 機能要件（要件IDは既存の F-01〜F-13 と衝突しない番号から）
   - ハブ連携仕様（`content_summary_cache` に書く `status` / `grade`（任意） / `badge` / `metrics` 最大6件 / `deep_link` の中身）
   - データモデル（タスクや予定と同じ「定義＋生成済み行」の2層構造が使えないか、まず検討する）

### 7.2 外部接続型の場合の実装手順
1. `server/services.js` の `AVAILABLE_SERVICES` に `{ content_key, display_name }` を1行追加する（これだけでハブの設定画面「外部サービス連携」に連携ボタンが出る）
2. ユーザーがハブの設定画面で「連携する」→ トークンが発行される
3. アドオン側はそのトークンを保存し（`hub_connections` 相当のテーブルを自分のDBに持つ。APP-2/APP-3の実装例を参照）、記録の作成・更新の都度、以下へ push する

   ```
   POST /api/integrations/summary
   Authorization: Bearer <トークン>
   { "target_date": "YYYY-MM-DD", "status": "...", "grade": "...", "badge": "...", "metrics": {...}, "deep_link": "..." }
   ```

4. DBは3章の方針どおり、ハブと同じ `life_manager` を共有し、`user_id` はハブの `users.id` をそのまま使うことを推奨する

### 7.3 内蔵型の場合の実装手順
1. `db/0N_◯◯.sql` にテーブルを追加する（`user_id` を持たせ `users.id` を参照する。繰り返しがあるなら「定義＋生成済み行」の2層構造を踏襲する）
2. `server/routes/◯◯.js` にAPIを実装し、`server/index.js` で `ensureDeviceUser` ミドルウェア付きでマウントする
3. `public/app.js` にタブ・画面を追加し、`public/index.html` のナビに追加する
4. その日の記録が変化するたびに `content_summary_cache` へ直接UPSERTする処理を実装する（`server/routes/summary.js` の当日サマリー等、ハブ側の表示にも必要に応じて追加する）
5. `server/middleware/auth.js` の新規端末作成処理に、必要な初期データ（既定のカテゴリ・カレンダー等）があれば追加する

### 7.4 共通の注意事項
- ハブ側（APP-1）のコードは、新しいアプリの**詳細を描画しない・編集しない**（`CLAUDE.md` 4.1）。増やしてよいのは要約カードだけ
- `metrics` は最大6件を厳守する
- 本ドキュメントの2章の表・6章の一覧・フェーズ計画（5章）に、追加したアプリを1行足して更新する
