# 要件定義書 — 食事管理アプリ（APP-2）

| 項目 | 内容 |
| --- | --- |
| 作成日 | 2026-09-17 |
| 状態 | ドラフト（`CLAUDE.md` から「要件定義済み・未着手」として参照されていたが、実体が失われていたため新規に起こした。実装着手前に内容の確認・合意が必要） |
| 参照 | `00_システム全体構成・連携仕様.md`（ハブとの連携方式） |

---

## 1. 目的

日々の食事内容と体重を記録し、栄養バランスを評価することで、生活管理アプリ（ハブ）の月次振り返りに「食」の観点を加える。単体では栄養管理アプリとして完結し、ハブには要約のみを渡す。

## 2. 責務範囲

### やること
- 食事の記録（いつ・何を・目安カロリー／栄養素）
- 体重の記録
- 日次・週次の栄養評価（採点・アドバイス的な短いフィードバック）
- ハブ（APP-1）向けに、その日の要約（grade・badge・metrics最大6件）を `content_summary_cache` に書き込む

### やらないこと（ハブ側の責務との境界）
- 収支管理・タスク管理は持たない（APP-1の責務）
- 他アプリ（運動管理）のデータは参照しない
- ハブ側の画面・データを直接操作しない（`content_summary_cache` への書き込みのみ）

## 3. 画面一覧（画面数：6、確定後は増やさない）

| ID | 画面 | 概要 |
| --- | --- | --- |
| A2-01 | 当日サマリー | 今日の摂取カロリー・栄養素の達成度、体重（当日記録があれば） |
| A2-02 | 食事記録 | 食事（朝/昼/夕/間食）の入力。食品名・量・目安カロリー・栄養素 |
| A2-03 | 体重記録 | 体重・体脂肪率（任意）の入力 |
| A2-04 | 履歴・カレンダー | 日別の記録一覧、月間の体重推移グラフ |
| A2-05 | 栄養評価レポート | 週次・月次の栄養バランス評価、目標との比較 |
| A2-06 | 設定 | 目標カロリー・目標体重・栄養目標（PFCバランス等）の設定 |

## 4. 機能要件

要件IDは APP-1（F-01〜F-03）と衝突しないよう F-04 以降を用いる。

### F-04：食事記録（A2-02）
- F-04-01 食事は「朝食・昼食・夕食・間食」の4区分＋日時を持つ
- F-04-02 食品はフリーテキスト入力を基本とする（食品データベース連携は将来検討、本フェーズでは手入力）
- F-04-03 カロリー・タンパク質・脂質・炭水化物は任意入力（未入力なら評価から除外）
- F-04-04 1日に何件でも記録できる
- F-04-05 記録の編集・削除ができる（論理削除。ハブと同じ方針に揃える）

### F-05：体重記録（A2-03）
- F-05-01 体重は1日1件を基本とするが、複数回記録した場合は最新値を採用する
- F-05-02 体脂肪率は任意項目
- F-05-03 前回記録からの増減を表示する

### F-06：栄養評価（A2-05）
- F-06-01 日次評価：目標カロリーに対する達成率、PFCバランスの目安との差分から grade（S/A/B/C）を算出する
- F-06-02 採点ロジックの具体的な閾値は未定。実装時にユーザーと合意する
- F-06-03 週次・月次では、日次gradeの平均・傾向を表示する

### F-07：ハブ連携（対 APP-1）
- F-07-01 その日の記録が1件でもある場合、`content_summary_cache` に以下を書き込む

  | フィールド | 値の例 |
  | --- | --- |
  | `content_key` | `'meal'` |
  | `status` | `'recorded'`（記録あり）／`'partial'`（体重のみ等）／`'none'`（記録なし） |
  | `grade` | F-06 の算出結果 |
  | `badge` | 例：`"目標達成"` `"体重-0.5kg"` など、あれば1つだけ |
  | `metrics` | 最大6件。例：`{"kcal":1850,"kcal_target_pct":92,"protein_g":72,"weight_kg":63.4,"weight_diff":-0.3,"pfc_score":"B"}` |
  | `deep_link` | A2-01（当日サマリー）への直リンク |

- F-07-02 記録の追加・編集・削除の都度、その日の行を再計算してUPSERTする
- F-07-03 ハブ側はこの要約を**表示するだけ**で、内訳の編集・詳細描画はしない（`00_システム全体構成・連携仕様.md` 4.2 参照）

## 5. データモデル（案・未確定）

具体的なカラム設計は実装着手時に確定するが、方向性は以下の通り。

| テーブル | 用途 |
| --- | --- |
| `meal_logs` | 食事記録（日時・区分・食品名・カロリー・PFC・メモ） |
| `weight_logs` | 体重記録（日付・体重・体脂肪率） |
| `nutrition_targets` | ユーザーごとの目標値（目標カロリー・PFC比率・目標体重） |

APP-1と同様、金額に相当する数値（カロリー等）は整数、体重のような小数が必要な値のみ `DECIMAL` を使う想定。日付の扱い・論理削除の方針はAPP-1のスキーマ規約（`db/01_schema.sql` の設計方針）に合わせる。

## 6. 非機能要件

- 食事記録の入力は、ハブの収支入力（3タップ・15秒以内）と同等の速さを目指す
- カレンダー・レポート画面は、ハブと同じく月単位でまとめて取得し、日ごとの通信を避ける

## 7. 未決定事項（実装着手前に確認）

- 採点ロジック（F-06）の具体的な計算式・閾値
- 食品データベース連携の要否（当面は手入力の想定）
- `metrics` に載せる6項目の最終確定（本書の例は仮）
- 認証・DBを APP-1 と共有するか（`00_システム全体構成・連携仕様.md` 3章参照）
