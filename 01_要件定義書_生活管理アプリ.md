# 要件定義書 — 生活管理アプリ（APP-1・ハブ）

| 項目 | 内容 |
| --- | --- |
| 作成日 | 2026-09-19（as-built：実装完了後に書き起こし） |
| 状態 | **実装済み。** 本書は「最初に立てた要件」ではなく、実際に動いているアプリの仕様を追認する形で作成した |
| 参照 | `CLAUDE.md`（設計制約）、`00_システム全体構成・連携仕様.md`（アドオン連携） |

---

## 1. 目的

収支とタスクという毎日の記録を通じて、月次の振り返りで QOL（生活の質）を確認できるようにする、個人向けの生活管理システムのハブアプリ。食事・運動・スケジュールなど他の観点は専用アプリ（アドオン）に任せ、本アプリはそれらの**要約を1箇所に集める窓口**として機能する。

## 2. 責務範囲

### やること
- 収支の記録・カテゴリ管理
- タスク（ルーティン／単発）の定義と、日々の実行チェック
- カレンダーによる月次の振り返り（収支・タスク完遂率）
- 記録の一覧・一括削除
- 他アプリ（食事・運動・スケジュールなど）の要約表示・連携管理

### やらないこと（`CLAUDE.md` 4.1）
- 食事・体重・運動などの記録機能そのものは持たない
- 他アプリの詳細（栄養グラフ、時系列リスト等）を描画しない
- 他アプリのデータを編集しない
- 受け取るのは要約（grade・badge・metrics 最大6件・deep_link）のみ
- 予算機能は作らない（`CLAUDE.md` 4.6、振り返りが基軸のため）

## 3. 画面一覧（画面数：9、確定・増やさない）

`CLAUDE.md` 4.2 により画面数は9に固定されている。ただし実装時のデザイン刷新（ユーザーフィードバックに基づく）により、**ナビゲーションは5タブに集約**し、9画面の内容はその中の各セクション・モーダルとして実現している。対応関係は以下の通り。

| ID | 画面 | 実装上の所在 | 概要 |
| --- | --- | --- | --- |
| A1-01 | 当日サマリー | 「今日」タブ上部（収支カード＋タスク完遂ゲージ） | 当日の収入・支出・収支、タスク完遂率を円形ゲージで表示 |
| A1-02 | 収支入力 | 「今日」タブの「＋収支」ボタン→モーダル | テンキー入力＋カテゴリ選択。起動から保存まで3タップ・15秒以内（`CLAUDE.md` 7章） |
| A1-03 | 当日タスク一覧 | 「今日」タブ中段（タスクリスト） | チェックボックスで実行トグル。押すたびに完遂ゲージ（A1-01）が連動して動く |
| A1-04 | タスク定義 | 「タスク」タブ（一覧→詳細→新規登録） | 既存タスクの一覧・詳細編集（色選択含む）・新規登録のみに限定し、タブ内遷移を増やさない |
| A1-05 | カレンダー | 「今日」タブのミニカレンダー | 月初に1回まとめて取得（性能要件）。日付タップで日別詳細へ |
| A1-06 | 日別詳細 | カレンダー日付タップ→モーダル | その日の収支明細・タスク実行状況・連携中サービスの要約を表示 |
| A1-07 | 月次サマリー | 「サマリー」タブ | カテゴリ別支出（円グラフ）、タスク完遂率、直近6か月の収支・達成率の推移（折れ線） |
| A1-08 | 記録一覧・一括編集 | 「記録」タブ | 月別の収支記録一覧、複数選択しての一括削除 |
| A1-09 | 設定 | 「設定」タブ | 端末情報、外部サービス連携、カテゴリ管理、タスクの完全削除 |

## 4. 機能要件

要件IDは食事管理アプリ（F-04〜F-07）・スケジュール管理アプリ（F-08〜F-13）と衝突しないよう、F-01〜F-03・F-14〜F-17を用いる。

### F-01：収支管理（A1-01, A1-02, A1-06, A1-08）
- F-01-01 収支は「支出」「収入」のいずれかの種別を持つ
- F-01-02 金額は円単位の整数（小数を扱わない、`CLAUDE.md` 5章）
- F-01-03 カテゴリは種別（支出/収入）ごとに設定し、ユーザーごとに独立して持つ
- F-01-04 入力はテンキー形式。カテゴリ選択→金額入力→保存の3タップで完了する
- F-01-05 メモは任意入力
- F-01-06 記録の削除は論理削除（`deleted_at`、`CLAUDE.md` 4.5）
- F-01-07 当日の収入・支出・収支はA1-01でリアルタイムに表示する
- F-01-08 日別の収支明細はA1-06（日別詳細）・A1-01（当日記録のアコーディオン）で確認できる
- F-01-09 記録一覧（A1-08）は月単位で全件表示し、チェックボックスによる複数選択・一括削除ができる

### F-02：タスク管理（A1-03, A1-04）
- F-02-01 タスクは「ルーティン」「単発」のいずれかの種別を持つ
- F-02-02 繰り返し条件は「毎日」「毎週（曜日指定）」「毎月（日付指定）」「単発（1回のみ）」から選ぶ
- F-02-03 タスクには表示色を設定できる（プリセットから選択）
- F-02-04 タスクの「予定」は、繰り返し条件からその都度計算するのではなく、`task_logs` に行として先に生成する（`CLAUDE.md` 4.4。分母をSQLで出すため）
- F-02-05 当日タスク一覧を開くたびに、その日の予定行をUPSERT（`INSERT IGNORE`）で生成する
- F-02-06 実行チェックはチェックボックスのトグルで行い、`completed_at` を `is_done` より先に更新する（MariaDBのUPDATE評価順対策、`CLAUDE.md` 5章）
- F-02-07 タスクを「やめる」場合は `end_date` を設定するのみで、削除しない（`CLAUDE.md` 4.3）
- F-02-08 タスクの物理削除は設定画面（A1-09）の奥にのみ置き、過去記録も消える旨の確認を挟む
- F-02-09 過去の `task_logs` は、タスク定義を変更しても一切変わらない（スナップショット方式）
- F-02-10 連続達成日数（ストリーク）を算出できる（`db/03_queries.sql` Q8）
- F-02-11 リマインド時刻を任意設定できる（通知の配信自体は本フェーズ対象外）

### F-03：振り返り（A1-05, A1-06, A1-07）
- F-03-01 カレンダー（A1-05）は月初に1回、収支とタスク完遂率をまとめて取得し、日ごとの通信を行わない（`CLAUDE.md` 7章）
- F-03-02 カレンダーの各日は、金額の数値表記ではなく色ドット（黒字＝緑、赤字＝赤）で収支の傾向を表す（視認性重視）
- F-03-03 日付タップで日別詳細（A1-06）をモーダル表示する。収支明細・タスク実行状況・連携中サービスの要約を含む
- F-03-04 月次サマリー（A1-07）はカテゴリ別支出を円グラフで表示する
- F-03-05 月次サマリーはタスクの月間完遂率を円形ゲージで表示する
- F-03-06 月次サマリーには直近6か月の「収支の推移」「タスク達成率の推移」を折れ線グラフで表示し、月ごとの比較ができるようにする
- F-03-07 連携中の他アプリ（食事・運動・スケジュール等）の要約カード（grade・badge・metrics最大6件・deep_link）をA1-01・A1-06に表示する。詳細描画・編集はしない（`CLAUDE.md` 4.1）

### F-14：記録一覧・一括編集（A1-08）
- F-14-01 月を指定して、その月の収支記録を日付降順で一覧表示する
- F-14-02 各記録をチェックボックスで複数選択できる
- F-14-03 選択した記録を一括で論理削除できる

### F-15：カテゴリ管理（A1-09）
- F-15-01 支出・収入それぞれのカテゴリを追加できる
- F-15-02 カテゴリは削除せず、非表示（`is_active=0`）に切り替える方式とする（収支管理と同じ論理削除思想）
- F-15-03 非表示のカテゴリは収支入力の選択肢に出ないが、過去記録の表示には影響しない

### F-16：端末識別（ログインなし）（全画面共通）
- F-16-01 ログイン画面は持たない。初回アクセス時にサーバーが端末（ブラウザ）ごとに `users` 行を自動作成する
- F-16-02 識別は署名付きCookie（`device_id`）に `user_id` を直接持たせる方式とし、サーバー再起動によるセッション消失の影響を受けない（`server/middleware/auth.js`）
- F-16-03 端末ごとにニックネームを設定できる（A1-09）
- F-16-04 端末のリセット（Cookie破棄）ができる。リセット後は次回アクセス時に新規端末として扱われ、以前のデータには戻れない

### F-17：外部サービス連携（ハブ側）（A1-09、A1-01、A1-06）
- F-17-01 連携可能なサービス一覧（`server/services.js` の `AVAILABLE_SERVICES`）を設定画面に表示する
- F-17-02 「連携する」操作でトークンを発行する。トークンは発行時に一度だけ表示し、以後は再表示しない
- F-17-03 発行したトークンは `service_connections` にハッシュ化して保存し、生の値は保持しない
- F-17-04 外部サービスは発行されたトークンで `POST /api/integrations/summary` に要約をpushする（`content_key` はトークンに固定され、他サービス領域には書けない）
- F-17-05 連携を解除すると、以後その要約カードは表示されなくなる（データ自体は `content_summary_cache` に残る）
- F-17-06 連携先ごとの要約は grade・badge・metrics（最大6件）・deep_linkのみを表示し、詳細は連携先アプリへの遷移（deep_link）で確認する

## 5. データモデル（実装済み）

| テーブル/ビュー | 用途 |
| --- | --- |
| `users` | 端末（ユーザー）。メール・パスワードは持たない |
| `categories` | 収支カテゴリ（`user_id` ごと） |
| `transactions` | 収支記録 |
| `task_definitions` | タスク定義（繰り返し条件・実行期間・色） |
| `task_logs` | タスクの予定と実行記録（スナップショット方式） |
| `content_summary_cache` | 他アプリからの要約キャッシュ（`content_key` ごと） |
| `service_connections` | 外部サービス連携のトークン管理 |
| `v_daily_balance` / `v_daily_task` / `v_calendar_cell` | カレンダー・サマリー用の集計ビュー |

いずれも `user_id` で分離されている（`db/04_multiuser.sql`）。詳細は各SQLファイル（`db/01_schema.sql`〜`db/05_service_connections.sql`）を参照。

## 6. 非機能要件（`CLAUDE.md` 7章・達成状況）

- カレンダー表示はローカル問い合わせ＋月次取得1回で完結 — **達成**（`GET /api/calendar?month=`）
- 日別画面を開くたびの通信は行うが、月をまたいだカレンダー全体の再取得は行わない — **達成**
- 収支入力は起動から保存完了まで3タップ・15秒以内 — **達成**（カテゴリ選択→テンキー→保存）
- 金額は円単位の整数、真偽値は0/1 — **達成**

## 7. 未決定・今後の検討事項

- タスクのリマインド通知の配信方法（ブラウザ通知等）は未実装。現状はリマインド時刻の保持のみ
- サーバー同期（Phase 2、PostgreSQL移行）は未着手
- metrics のキー命名規則の全アプリ共通化は未決定（`00_システム全体構成・連携仕様.md` 4.4）
