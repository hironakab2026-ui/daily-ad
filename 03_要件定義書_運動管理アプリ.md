# 要件定義書（骨子）— 運動管理アプリ（APP-3）

| 項目 | 内容 |
| --- | --- |
| 作成日 | 2026-09-17 |
| 状態 | **骨子のみ。詳細は未定。** `CLAUDE.md` の記載どおり、APP-2 の接続（Phase 3）が終わってから詳細化する想定 |
| 参照 | `00_システム全体構成・連携仕様.md`（ハブとの連携方式） |

---

## 1. 目的（ラフ）

筋力トレーニングの記録と評価を行い、ハブの月次振り返りに「運動」の観点を加える。APP-2（食事管理）と同じく、ハブとは要約のみで連携する。

## 2. 責務範囲（ラフ）

- やること：筋トレの記録（種目・重量・回数・セット数）、トレーニング内容の評価、ハブへの要約送出
- やらないこと：収支・タスク・食事のデータは持たない／触らない

## 3. 画面イメージ（未確定・4〜6画面程度を想定）

| 仮ID | 画面 | メモ |
| --- | --- | --- |
| A3-01 | 当日サマリー | 今日のトレーニング有無、直近の傾向 |
| A3-02 | トレーニング記録 | 種目・重量・回数・セットの入力 |
| A3-03 | 種目マスタ管理 | よく使う種目の登録（任意） |
| A3-04 | 履歴・評価 | 週次のボリューム（重量×回数×セット）推移など |

画面数・構成はAPP-2の実装経験を踏まえて確定する（先に作るAPP-2の設計パターンを流用できる部分が多いと想定）。

## 4. データモデル（方向性のみ）

| テーブル | 用途（案） |
| --- | --- |
| `exercises` | 種目マスタ（種目名、対象部位など） |
| `workout_logs` | 1セット単位の記録（種目・重量・回数・日時） |

正規化の粒度（セット単位で持つか、1トレーニング＝1行で持つか）は未定。

## 5. ハブ連携（イメージ）

`00_システム全体構成・連携仕様.md` の `content_summary_cache` を使う点はAPP-2と同じ。

- `content_key` は `'workout'`
- `metrics` の例（仮）：今週のトレーニング日数、総ボリューム、主要種目の重量推移など、最大6件
- 採点基準（grade）は未定

## 6. 未決定事項（多数・詳細化はPhase 4想定）

- セット・種目の記録粒度
- 評価（grade）の算出方法
- 種目マスタを個人ごとに持つか、共通マスタを用意するか
- 画面数・具体的なUI
- 実装着手のタイミング（APP-2完了後を想定）

本書はAPP-2の要件が固まり、実装・連携の実績ができた段階で改めて詳細化する。
