# 要件定義書 — スケジュール管理アプリ（APP-4）

| 項目 | 内容 |
| --- | --- |
| 作成日 | 2026-09-18 |
| 状態 | ドラフト（新規要件定義。実装着手前にユーザーとの確認・合意が必要） |
| 参照 | `00_システム全体構成・連携仕様.md`（ハブとの連携方式）、`CLAUDE.md`（設計上の制約） |

---

## 1. 目的

時間単位の予定（会議・約束・作業ブロックなど）を記録・可視化する、Google カレンダーのようなスケジュール管理アプリ。生活管理アプリ（ハブ・APP-1）の**付属アプリ（アドオン）**として位置づけ、ハブの月次振り返りに「時間の使い方」の観点を加える。単体でも完結したカレンダーアプリとして使えることを目指す。

ハブの A1-05（カレンダー画面）とは目的が異なる点に注意する。

| | ハブ A1-05（既存） | 本アプリ（新規） |
| --- | --- | --- |
| 見るもの | 日ごとの収支・タスク完遂率 | 時刻ごとの個々の予定 |
| 単位 | 1日1セル（月表示のみ） | 時間軸（週表示・日表示が中心） |
| データ | `transactions` / `task_logs` | このアプリが新設するテーブル |

両者は競合しない。本アプリはハブに**要約**のみを渡し、ハブ側の A1-05 は今まで通り収支・タスクの表示に専念する（`CLAUDE.md` 4.1）。

## 2. 責務範囲

### やること
- 時刻を持つ予定（開始・終了時刻、または終日）の作成・編集・削除
- 予定の繰り返し（毎日・毎週・毎月など）
- 予定ごとのリマインド（通知）
- 用途別カレンダー（仕事・プライベート等）の色分け管理
- 月表示・週表示・日表示でのカレンダー閲覧
- ハブ（APP-1）向けに、その日の要約（grade・badge・metrics 最大6件）を `content_summary_cache` に書き込む

### やらないこと（ハブ側の責務との境界）
- 収支管理・タスク管理は持たない（APP-1 の責務）
- 他アプリ（食事・運動）のデータは参照しない
- ハブ側の画面・データを直接操作しない（`content_summary_cache` への書き込みのみ、`00_システム全体構成・連携仕様.md` 4.1 参照）
- ハブの A1-05／A1-06 の表示を変更しない

## 3. 画面一覧（画面数：7、確定後は増やさない）

| ID | 画面 | 概要 |
| --- | --- | --- |
| A4-01 | 週表示（メイン画面） | 起動時のデフォルト画面。時間軸 × 7日のグリッドで予定をブロック表示 |
| A4-02 | 日表示 | 週表示と同じ時間軸で1日分。予定が多い日の詳細確認用 |
| A4-03 | 月表示 | 月間の見通し確認用。日ごとのセルに予定タイトルを少数表示 |
| A4-04 | 予定作成・編集 | タイトル・日時・場所・メモ・カレンダー（色分け）・繰り返し・リマインドの入力 |
| A4-05 | 予定詳細 | グリッド上の予定をタップした際の詳細表示（編集・削除への入口） |
| A4-06 | カレンダー管理 | 用途別カレンダー（仕事／プライベート等）の作成・色設定・表示ON/OFF |
| A4-07 | 設定 | 週の開始曜日、デフォルト表示、既定のリマインド時刻、ハブ連携設定 |

### 3.1 週表示のイメージ（視覚仕様）

```
       月    火    水    木    金    土    日
      ─────────────────────────────────────
 9:00 │     │会議  │     │     │     │     │
      │     │[青] │     │     │     │     │
10:00 │─────┼─────┼─────┼─────┼─────┼─────┤
      │作業  │会議  │     │作業  │     │     │
11:00 │[緑] │[青] │     │[緑] │     │     │
      │     ├─────┤     ├─────┤     │     │
12:00 │     │     │     │     │     │     │
      ══════════════════════════════════════  ← 現在時刻ライン（now line）
13:00 │     │     │ランチ│     │     │     │
      │     │     │[黄] │     │     │     │
```

- 予定は所要時間に応じてブロックの高さが変わる（15分単位でスナップ）
- 予定の色はカレンダー（A4-06 で設定した用途別カレンダー）の色を継承する
- 現在時刻に横線を表示し、「今」がひと目でわかるようにする
- 終日予定はグリッド上部の専用レーンに帯で表示する（Google カレンダーと同様）
- 予定が重なる時間帯は横に並べて表示する

## 4. 機能要件

要件 ID は既存アプリ（F-01〜F-07）と衝突しないよう F-08 以降を用いる。

### F-08：予定の作成・編集（A4-04）
- F-08-01 予定はタイトル（必須）・開始日時・終了日時を持つ
- F-08-02 終日予定に対応する（開始・終了時刻を持たず、日付のみ）
- F-08-03 場所・メモは任意入力
- F-08-04 予定は必ずいずれか1つのカレンダー（A4-06）に属し、色を継承する
- F-08-05 同じ時間帯に複数の予定があってもブロックしない（予定の重複を許容する）
- F-08-06 編集・削除は論理削除とする（`deleted_at`。ハブと同じ方針、`CLAUDE.md` 4.5）

### F-09：繰り返し予定
- F-09-01 毎日・毎週（曜日指定）・毎月（日付指定）・毎年の繰り返しに対応する
- F-09-02 繰り返しの終了条件は「回数指定」「終了日指定」「終了なし」から選べる
- F-09-03 **繰り返し予定も、ハブのタスク（`CLAUDE.md` 4.4）と同じ考え方で、繰り返し条件からその都度計算するのではなく、各回の予定行をあらかじめ生成しておく方式とする。**
  理由：週表示・月表示で範囲検索するたびに繰り返しルールを展開すると重く、実装も複雑になる。行として先に持てば単純な日付範囲の SQL で済み、後述 F-09-04 の「1回だけ変更」も自然に表現できる
- F-09-04 生成済みの回のうち1回だけを変更・削除できる（その回だけ例外扱いにする。以降の回や過去の回には影響しない）
- F-09-05 繰り返し定義そのものを変更した場合、**過去に生成済みの回は変更しない**（`CLAUDE.md` 4.3 の「過去の task_logs は定義を変更しても一切変わらない」と同じ方針）。未来分のみ再生成する

### F-10：リマインド（通知）
- F-10-01 予定ごとに通知タイミングを設定できる（例：5分前／10分前／30分前／1時間前／当日朝）。複数設定可
- F-10-02 通知の配信方法（ブラウザ通知・アプリ内バナー・メール等）は本フェーズでは未定。まずはアプリを開いている間のアプリ内通知を基本線とする（F-16 未決定事項参照）

### F-11：カレンダー管理（A4-06）
- F-11-01 複数のカレンダー（用途別グループ、例：仕事／プライベート／家族）を作成できる
- F-11-02 カレンダーごとに表示色を設定する（あらかじめ用意した配色候補から選ぶ）
- F-11-03 カレンダーごとに週表示・月表示での表示／非表示を切り替えられる
- F-11-04 カレンダーの削除は論理削除とし、所属する予定も合わせて非表示にする（過去の記録は消えない、`CLAUDE.md` 4.3 と同じ考え方）

### F-12：表示（週・日・月）
- F-12-01 起動時は週表示（A4-01）を初期画面とする
- F-12-02 週表示・日表示は同じ時間軸コンポーネントを使う（表示日数が異なるだけ）
- F-12-03 月表示（A4-03）は日ごとのセルに予定タイトルを最大3件表示し、それ以上は「他n件」とまとめる
- F-12-04 月表示から日付をタップすると日表示（A4-02）に遷移する
- F-12-05 直前に見ていた表示モード（週／日／月）を記憶し、次回起動時もそこから始める

### F-13：ハブ連携（対 APP-1）
- F-13-01 既存の連携の仕組み（README 記載の `/api/connections` によるトークン発行、`/api/integrations/summary` へのプッシュ）をそのまま利用する
- F-13-02 その日に予定が1件でもある場合、`content_summary_cache` に以下を書き込む

  | フィールド | 値の例 |
  | --- | --- |
  | `content_key` | `'schedule'` |
  | `status` | `'recorded'`（予定あり）／`'none'`（予定なし） |
  | `grade` | 未定（F-14 未決定事項参照。無理に算出せず固定値でもよい） |
  | `badge` | 例：`"会議3件"` など、あれば1つだけ |
  | `metrics` | 最大6件。例：`{"event_count":4,"busy_hours":3.5,"next_event_in_min":45,"all_day_count":0}` |
  | `deep_link` | A4-01（週表示）の当日部分への直リンク |

- F-13-03 予定の追加・編集・削除の都度、その日の行を再計算して UPSERT する
- F-13-04 繰り返し予定が複数日にまたがる週表示の要約を出す場合、生成済みの行（F-09-03）を日付で単純に集計すればよい
- F-13-05 ハブ側はこの要約を**表示するだけ**で、内訳の編集・時間軸の描画はしない（`00_システム全体構成・連携仕様.md` 4.2）

## 5. データモデル（案・未確定）

具体的なカラム設計は実装着手時に確定するが、方向性は以下の通り。ハブのタスク機能（`task_definitions` / `task_logs`）と同じ「定義＋生成済み行」の2層構造を踏襲する。

| テーブル | 用途 |
| --- | --- |
| `schedule_calendars` | 用途別カレンダー（名前・色・表示ON/OFF・論理削除） |
| `schedule_definitions` | 予定の定義（タイトル・場所・メモ・所属カレンダー・単発 or 繰り返し条件・実行期間） |
| `schedule_occurrences` | 生成済みの回（開始・終了日時・終日フラグ・定義への参照・「この回だけ例外」フラグ・論理削除） |
| `schedule_reminders` | リマインド設定（対象の定義または回・通知タイミング（分前）） |

- 日時は APP-1 の日付文字列方式に倣うか、時刻を含むため別形式（`DATETIME` 相当）にするかは DB 選定（`db/05_データベース選定メモ.md`）と合わせて確定する
- 論理削除・繰り返しの生成方式は APP-1 のスキーマ規約（`db/01_schema.sql` の設計方針、`CLAUDE.md` 4.3〜4.5）に合わせる

## 6. 非機能要件

- 週表示・日表示への切り替えは、画面内で完結し通信を伴わない体感速度を目指す（表示範囲分のデータは事前にまとめて取得する）
- 月表示はハブの A1-05 と同様、月単位でまとめて取得し、日ごとの通信を避ける
- 予定作成は、ハブの収支入力（3タップ・15秒以内）に準じた速さを目指す。ただし繰り返し設定など入力項目が多い操作は例外とする
- タイムゾーンは端末ローカルを前提とする（複数タイムゾーンの相互変換は本フェーズでは扱わない）

## 7. 未決定事項（実装着手前に確認）

- **画面数・機能の過不足**：7画面案（A4-01〜07）で過不足がないか
- **grade の扱い（F-13-02）**：食事・運動アプリのような「採点」がスケジュール管理になじむか。無理に grade を作らず `status` と `metrics` のみにする案も検討の余地あり
- **通知の配信方法（F-10-02）**：ブラウザ通知（要 Service Worker・許可ダイアログ）まで対応するか、アプリを開いている間のみの通知にとどめるか
- **繰り返し予定の生成範囲**：ハブのタスクは「実行期間」を持つが、本アプリの繰り返し予定もどこまで先の回を事前生成しておくか（例：直近3ヶ月分など）を決める必要がある
- **日時の型・タイムゾーンの扱い**：`db/05_データベース選定メモ.md` の DB 選定と合わせて確定する
- **認証・DB を APP-1 と共有するか**：`00_システム全体構成・連携仕様.md` 3章と同じ論点（本アプリも同じ MariaDB インスタンス・`users` テーブルを共有する案が有力）
- **`00_システム全体構成・連携仕様.md` への反映**：本アプリを APP-4 として構成図・フェーズ計画に追記するかどうか（承認後に反映する）
